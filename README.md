# Bioregional Passport

A multi-bioregion platform for place-based identity, trust, funding and exchange. Each bioregion runs its own **pod** on shared open protocols. See `docs/build-set/` for the PRD, architecture, protocol reference and implementation plan, and `docs/plans/` for the current session plan.

Apache-2.0.
